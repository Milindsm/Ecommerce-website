import React from "react";
import AvailableProducts from "../../Navbar/AvailableProducts";

const Store = () => {
    return (
        <div>
            <AvailableProducts/>
        </div>
    );
};

export default Store;